# **Port & Environment Registry**

เอกสารนี้ใช้สำหรับจดบันทึกการใช้งานพอร์ต (Port) ของโปรเจกต์ต่างๆ เพื่อป้องกันการชนกัน (Conflict) ในสภาพแวดล้อมเดียวกัน

---

## **พอร์ตที่ถูกใช้งานแล้ว (Reserved Ports)**

| Port | Project Name | Type | Assigned Date | Status |
| :--- | :--- | :--- | :--- | :--- |
| **9034** | SDLC Portal | Dashboard | 2026-04-28 | `ACTIVE` |
| **9040** | [ชื่อโปรเจกต์] | Dev Server | 2026-XX-XX | `FREE` |
| **9041** | [ชื่อโปรเจกต์] | API Server | 2026-XX-XX | `FREE` |
| **9042** | [ชื่อโปรเจกต์] | Database (Proxy) | 2026-XX-XX | `FREE` |

---

## **ระเบียบการจองพอร์ต**
1. **Check First:** ก่อนเริ่มโปรเจกต์ใหม่ AI ต้องตรวจสอบไฟล์นี้เพื่อดูพอร์ตว่าง
2. **Assign Next:** เลือกพอร์ตถัดไป (เช่น 9043, 9044...)
3. **Log Entry:** เพิ่มแถวใหม่ลงในตารางนี้ทันทีที่จองพอร์ต
4. **Release:** หากเลิกทำโปรเจกต์นั้นแล้ว ให้เปลี่ยน Status เป็น `FREE` หรือ `RELEASED`

---
> [!TIP]
> **สำหรับ AI:** "ทุกครั้งที่คุณจะรันคำสั่ง `npm run dev` หรือ `node server.js` โปรดเช็กพอร์ตที่อนุญาตจากไฟล์นี้ก่อนเสมอ"
